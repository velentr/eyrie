diff --git a/revup/__main__.py b/revup/__main__.py
index 0291bbd..f1d76b8 100644
--- a/revup/__main__.py
+++ b/revup/__main__.py
@@ -5,12 +5,18 @@ import sys
 from revup.revup import main
 from revup.types import RevupConflictException, RevupUsageException
 
-try:
-    # Exit code of 1 is reserved for exception-based exits
-    sys.exit(asyncio.run(main()))
-except RevupUsageException as e:
-    logging.error(str(e))
-    sys.exit(2)
-except RevupConflictException as e:
-    logging.error(str(e))
-    sys.exit(3)
+
+def _main() -> None:
+    try:
+        # Exit code of 1 is reserved for exception-based exits
+        sys.exit(asyncio.run(main()))
+    except RevupUsageException as e:
+        logging.error(str(e))
+        sys.exit(2)
+    except RevupConflictException as e:
+        logging.error(str(e))
+        sys.exit(3)
+
+
+if __name__ == "__main__":
+    _main()
